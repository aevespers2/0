# Gods and Clan Integration

Adds Jira and Prometheus under `Gods`, Terraform tooling under `Clan`, and clan flag `ralbane`. All work remains draft until exact-head CI passes and configured credentials are reviewed.
