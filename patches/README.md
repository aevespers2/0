# Patch Exchange

Safari/cloud Codex and advisory surfaces should export patches, plans, and diffs here.

Patch proposals must include:

- `source`
- `repo`
- `base`
- `type`
- `summary`
- `files`

Local CLI validates and applies patches. Local CLI remains the only authoritative
GitHub write agent for commits, mirror pushes, CI verification, and deployments.
